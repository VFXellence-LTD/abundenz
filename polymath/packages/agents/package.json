{
  "name": "@polymath/agents",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Polymath agent engines — Viral clip draft generator (script-level MVP)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "viral:run": "node --import tsx bin/viral-run.ts",
    "viral:render": "node --import tsx bin/viral-render.ts"
  },
  "engines": { "node": ">=22" },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.22.3",
    "typescript": "~6.0.2",
    "vitest": "^4.1.7"
  }
}
