# Changelog — 2026-06-14

## Plan 3 — PTY Claude-Session Engine Driver (Tasks 1–12)

### Status: COMPLETE (Tasks 1–11 delivered; Task 12 deferred — CampaignControlPanel is Plan 4 deliverable)

### Dependencies resolved

| Package | Resolved version | Notes |
|---------|-----------------|-------|
| `node-pty` | 1.1.0 | Server dep. Native binding rebuilds automatically against the running Node ABI on `pnpm install`. No manual `pnpm rebuild` needed on Node 20/22 win32 with `@homebridge/node-pty-prebuilt-multiarch` prebuilds. See knowledge capture below. |
| `xterm` | 5.3.0 | Client dep. |
| `@xterm/addon-fit` | 0.10.0 | Client dep. |
| `@xterm/addon-web-links` | 0.11.0 | Client dep. |
| `ws` | already in server (8.x) | Promoted from stub to real use. |

### opt-in: MC_PTY_SMOKE=1

The `server/test/pty-ws.smoke.test.ts` end-to-end PTY/WS integration test is **gated** behind the env flag `MC_PTY_SMOKE=1`. Without it the test is skipped (`1 skipped` in the suite). To run manually in PowerShell:

```powershell
$env:MC_PTY_SMOKE=1; pnpm --dir ".canon\.mission-control\server" exec vitest run test/pty-ws.smoke.test.ts; Remove-Item Env:MC_PTY_SMOKE
```

Expected: `1 passed` — echoes a marker string over the WebSocket.

### AgentRunsService.setStatus — additive change

`setStatus(id, status, error?)` gained an optional `error` string parameter. Existing 2-arg callers are unaffected (backward-compatible). The error column uses `COALESCE(?, error)` so only non-null values overwrite.

### Plan 2 dependency — Task 12

`CampaignControlPanel.tsx` was not present in this worktree at Plan 3 execution time. Task 12 (rewire Run button to `/api/sessions/start`) is **deferred to Plan 4** when the campaign control panel is rebuilt. The Sessions board (Task 11) provides a fully working path to start sessions independently of the panel.

### Test suite result

| Suite | Files | Tests | Result |
|-------|-------|-------|--------|
| Server (vitest) | 15 passed, 1 skipped | 75 passed, 1 skipped | Green |
| Client (tsc) | — | — | Clean |
| Client (vite build) | — | — | Clean (1 xterm chunk size warning, not an error) |

### Commit SHAs (on feat/pty-driver)

| Task | SHA | Summary |
|------|-----|---------|
| Task 1 | bf5832c | Add MC logger service and node-pty dependency |
| Task 2 | f384609 | Add PtyService node-pty wrapper with scrollback |
| Task 3 | f28bf7a | Add SessionService lifecycle, idle detect, allowlist |
| Task 4 | 4bcdeae | Add /ws/terminal PTY I/O bridge |
| Task 5 | 137d4dc | Add ClaudeSessionsService to resolve a run's session UUID |
| Task 6 | d58510a | Add /api/sessions start/list/stop with approval gate |
| Task 7 | e5d9f35 | Add gated PTY/WS integration smoke (harmless echo) |
| Task 8 | 917206f | Add buildSurgePrompt seeding from .canon viral doctrine |
| Task 9 | 772d512 | Wire PTY/session services and upgrade /ws to broadcast |
| Task 10 | 944fb81 | Add client EmbeddedTerminal (xterm) and useWebSocket hook |
| Task 11 | 231e7b1 | Add Sessions board surface, route, and nav |
| Task 12 | — | Deferred |

---

## Plan 4: Surge Engine (Tasks 7-11)

Branch: `feat/surge-engine`

### T7 — runGenerate driver + surge-run CLI
- Files: `src/driver.ts`, `bin/surge-run.ts`, `src/index.ts` (final barrel), `test/driver.test.ts`
- Tests: 3 new (driver: clean run, BLOCKED logged, malformed rejects) → total 25
- Plan-code bugs fixed:
  - `src/validateDraft.ts:32,38` — `as Record<string,unknown>` → `as unknown as Record<string,unknown>` (TS strict mode cast error)
  - `test/driver.test.ts` — `good()` fixture script was 24 words, failing `validateDraft`'s 50-word floor; padded to 89 words
- Outcome: full pipeline validate→safeguard→writeDraft→recordApproval→HALT; exactly 2 fetch calls (POST approvals + PATCH task); no publish

### T8 — /surge-generate SKILL.md
- File: `.canon/.claude/skills/polymath/surge-generate/SKILL.md`
- Frontmatter: `# Skill:` title + fenced yaml block (matches content-atomizer convention)
- HALT line present: "HALT. No publish, no social accounts, no render."
- Outcome: LLM brief for spawned session; delegates safeguard/write/record/halt to tested surge-run CLI

### T9 — /surge-safeguard-check SKILL.md
- File: `.canon/.claude/skills/polymath/surge-safeguard-check/SKILL.md`
- Read-only policy re-check; no artifact write, no record, no publish
- HALT line present: "HALT. No publish, no social accounts, no render."

### T10 — /surge-continue SKILL.md
- File: `.canon/.claude/skills/polymath/surge-continue/SKILL.md`
- Resume loop post-rejection; uses prior reviewNotes; same hard scope as surge-generate
- HALT line present: "HALT. No publish, no social accounts, no render."

### T11 — Workspace wiring + final verification
- `@polymath/agents` confirmed in pnpm workspace (`pnpm ls --filter @polymath/agents`)
- Full suite: 7 test files, 25 tests, all pass
- Typecheck: `tsc --noEmit` exits 0

## Plan 4 status: CLOSED
The MVP loop is complete: generate ONE Zrodinger clip draft → safeguard gate → write artifact (markdown + json) → POST pending approval → PATCH task to in-review → HALT. No publish, no accounts. Human reviews in Mission Control Approval Queue.

---

## Plan 5: Render Pipeline Wiring (T10-T13)

Branch: `feat/render-pipeline`

### T10 — GET /api/artifacts/:id route
- Add `artifactsRoot` to `config.ts` (defaults to `polymath/packages/agents/artifacts`)
- New service `artifacts.service.ts`: resolves approval `preview_url`, path-confined to `artifactsRoot`, traversal rejected with 400
- New route `routes/artifacts.ts`: streams file with correct MIME type (mp4/png/mp3), 404 for missing/unknown, 400 for traversal
- `AppDeps` extended with optional `artifactsRoot`; route mounted at `/api/artifacts`
- Tests: 4 new (valid serve, traversal 400, unknown id 404, missing file 404) → server total 79/80

### T11 — ContentPreview video branch
- Extend `ApprovalContent` in `engine.ts` with `videoPath`, `thumbnailPath`, `durationSec`, `renderReport`
- `ContentPreview.tsx`: new `approvalId` + `contentType` props; video branch renders `<video src=/api/artifacts/:id>` with DRY-RUN badge from `renderReport`; existing script branch unchanged
- `ApprovalCard.tsx`: passes `approvalId={approval.id}` and `contentType={approval.contentType}` to ContentPreview
- Tests: 2 new (video branch renders `<video>`, clip branch keeps script preview) → client total 22/22
- TypeScript: `tsc --noEmit` exits 0

### T12 — /surge-render SKILL.md
- File: `.canon/.claude/skills/polymath/surge-render/SKILL.md`
- Runs only on `approved` `content_type='clip'` rows; HALT after render
- Boss-approve-before-spend doctrine documented for paid ElevenLabs TTS path
- Output: `RenderedClip` mp4+thumbnail + second `pending` `content_type='video'` approval row

### T13 — /surge-render PTY allowlist
- `session.service.ts`: `/surge-render` added to `ALLOWED_SKILLS`
- `session.service.test.ts`: test updated to "four surge skills"; `aq_abc_123` argument accepted

## Plan 5 status: CLOSED
Render pipeline wired: approved clip draft → render driver → mp4 artifact → `/api/artifacts/:id` serves it → ContentPreview shows `<video>` in approval queue → Boss reviews rendered video before publish.

---

## Plan 6: Publish Layer — Package Tasks (Tasks 1-4)

Branch: `feat/publish-layer`

### T1 — Scaffold + types + DryRunDistributor (`be71048`)
- New package `@polymath/publish` at `packages/publish/`
- `src/types.ts`: `PublishClip`, `PublishTarget`, `PublishResult`, `Distributor`, `BrandCreds` interfaces
- `src/DryRunDistributor.ts`: logs `WOULD PUBLISH to <platform>: <caption>`, returns `{dryRun:true, status:"skipped"}`
- `vitest.config.ts`, `tsconfig.json`, `package.json` scaffolded
- Note: `@polymath/types` workspace dep removed — package not yet created; local `types.ts` provides all contracts
- Tests: 2 pass

### T2 — Per-brand credential resolution (`7647e22`)
- `src/credentials.ts`: `resolveBrandCreds(ecosystemId, env)` — suffixed env keys (`BUFFER_TOKEN__VIRAL`) only
- Bare `BUFFER_TOKEN` (no suffix) rejected to prevent cross-brand bleed
- `provider:null` default → dry-run path
- Tests: 4 pass (affiliate-never-inherits-viral confirmed)

### T3 — Buffer + Postiz adapters (`9025913`)
- `src/BufferDistributor.ts`: GraphQL POST to Buffer API, Bearer token auth, returns `failed` on non-ok (never throws)
- `src/PostizDistributor.ts`: REST POST to `postizApiUrl/api/posts`, `x-api-key` header, returns `failed` on non-ok
- Both tested with `vi.stubGlobal("fetch", ...)` — no real network calls
- Tests: 4 pass (2 per adapter)

### T4 — Distributor factory (`945567c`)
- `src/factory.ts`: `selectDistributor(ecosystemId, env)` resolves brand creds → returns typed adapter
- Default: `DryRunDistributor` when no creds; live adapters only when suffixed env keys present
- Brand isolation confirmed: viral creds never produce a live distributor for affiliate
- Tests: 4 pass

## Plan 6 package tasks status: COMPLETE
`@polymath/publish` package live with 14 tests across 5 test files. Full suite green. DryRun is default — no social publish happens without explicit per-brand env keys set by Boss.

---

## Plan 6: Publish Layer — Server + Client + Docs (Tasks 5-7)

Branch: `feat/publish-layer`

### T5 — Server publish gate + route
- `db.ts`: `publish_log` table added (idempotent `CREATE TABLE IF NOT EXISTS`), index `idx_publish_log_approval`
- `tasks.service.ts`: `"published"` added to `TaskStatus` union and `TASK_STATUSES` array
- `services/publish.service.ts`: `PublishService` class — gate (approved video only, FTC guard for affiliate), builds `PublishClip` from approval `content_json`, calls `selectDistributor`, writes `publish_log` row per platform, sets task to `published`
- `routes/publish.ts`: `POST /api/publish` — 400 missing, 404 not found, 409 conflict, 200 with results array
- `index.ts`: router mounted at `/api/publish`
- Server package.json: `@polymath/publish` added as `file:../../../polymath/packages/publish`
- `@polymath/publish/package.json`: `exports` field added pointing to `./src/index.ts` for tsx resolution
- `server/vitest.config.ts`: created to anchor vitest to server dir (prevents config walk-up to parent)
- Tests: `test/publish.service.test.ts` (4 tests), `test/publish.route.test.ts` (5 route + 3 doctrine guard tests), `test/db.test.ts` updated to 12 tables
- Server total: 91 tests, 0 failures

### T6 — Client PublishButton
- `client/src/features/approvals/PublishButton.tsx`: renders only for `contentType=video AND status=approved`; POST /api/publish on click; per-platform results list; DRY RUN — not actually posted banner when `dryRun:true`; inline error on failure
- `client/src/features/approvals/PublishButton.test.tsx`: 5 tests (gate, render, dry-run banner, platform result)
- `client/src/features/approvals/ApprovalCard.tsx`: `<PublishButton approval={approval} />` mounted after buttons row
- Client total: 27 tests, 0 failures; `tsc --noEmit` clean

### T7 — Surge publish skill + go-live handoff
- `.canon/.claude/skills/polymath/surge-publish/SKILL.md`: HALT gate documented, dry-run default, env var naming convention, doctrine reminders
- `.canon/5_knowledge/reference/polymath-business/GO-LIVE-HANDOFF.md`: Boss-only 5-step sequence (accounts, Buffer/Postiz, env vars, restart, click), FTC note, "What Claude Never Does" section

## Plan 6 status: COMPLETE
Branch `feat/publish-layer` ready for merge to develop. Human-gated publish pipeline: Boss approves video in Approval Queue → clicks Publish button → server gates on approved video → dry-run distributor by default → logs publish_log row → sets task published → dashboard shows per-platform result with DRY RUN banner.

---

## Chore / Go-Live Task Seed

Added `.canon/.mission-control/server/scripts/seed-chores.ts` — idempotent seed script that populates open chores and go-live blockers into the Mission Control SQLite board.

**First run:** 7 inserted. **Second run (idempotency check):** 0 inserted.

| ID | Type | Priority | Status | Title |
|----|------|----------|--------|-------|
| INFRA-001 | infra | low | backlog | Delete orphan git worktree dirs |
| INFRA-002 | infra | medium | backlog | Fix server-side brand registration |
| INFRA-003 | infra | low | backlog | Update stale CLAUDE.md routing tables |
| GOLIVE-001 | setup | high | todo | Create @zrodinger social accounts |
| GOLIVE-002 | setup | high | todo | Connect Buffer/Postiz or obtain platform tokens |
| GOLIVE-003 | setup | high | todo | Set Mission Control env vars (creds, never in vault) |
| GOLIVE-004 | setup | high | todo | Approve paid-tool spend (ElevenLabs; Buffer/Postiz) |

Note: `ecosystem_id NOT NULL` constraint — INFRA tasks use `"apps"` bucket as infra/system stand-in.

E2E cleanup: no test rows found (db was freshly created; campaigns, approval_queue, publish_log all empty).
