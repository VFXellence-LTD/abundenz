# 2026-06-13 — VFXellence-LTD Changelog

## RESTRUCTURE — monorepo re-root + .canon merge

### Overview

Completed Plan 0: the entire VFXellence-LTD project surface was restructured from two separate roots (`D:\VFXellence-LTD\polymath` as git root + `.canon` as a sibling directory) into a single unified monorepo rooted at `D:\VFXellence-LTD`. The `.canon` governance vault was also restructured to absorb all polymath business content from `polymath/vault`.

---

### Git Root Change

- **Before:** git root was `D:\VFXellence-LTD\polymath`
- **After:** git root is `D:\VFXellence-LTD`
- **Branch:** `develop` (8 commits ahead of `origin/develop`)
- **Remote:** `github.com/VFXellence-LTD/polymath-dashboard` (private) — rename decision deferred

---

### Commit Sequence (restructure arc)

| SHA | Description |
|-----|-------------|
| `610126c` | Re-root project as single monorepo at VFXellence-LTD |
| `fe4722a` | Create .canon polymath-business slots + codebase stubs |
| `f86bc2e` | Merge vault/dev into .canon (profiles, changelogs, standards, plans) |
| `331a853` | Merge polymath business vault into .canon paradigms |
| `f1b9778` | Move polymath skills into .canon/.claude/skills |
| `0977728` | Move dashboard to .canon/.mission-control/client |
| `f814b9d` | Repoint polymath profile, dev script, and poly.bat to new layout |
| `1c9f7b4` | Convert polymath/vault to a pointer to .canon |
| *(this commit)* | Record monorepo restructure in changelog |

---

### What Moved

| Content | Source | Destination |
|---------|--------|-------------|
| 696 vault files (business specs, ecosystems, agents, workflows, playbooks) | `polymath/vault/` | `.canon/2_architect/polymath-business/` + `.canon/1_controller/standards/polymath-business/` + `.canon/5_knowledge/` |
| Dashboard (Vite + React app) | `polymath/apps/dashboard/` | `.canon/.mission-control/client/` |
| 4 polymath skills (polymath-pitfalls, niche-locker, content-atomizer, review-miner) | `polymath/vault/shared/skills/` | `.canon/.claude/skills/polymath/` |
| Dev profiles, changelogs, standards, plans | `polymath/vault/dev/` | `.canon/1_controller/profiles/polymath/`, `.canon/4_orchestrator/changelogs/`, `.canon/1_controller/standards/`, `.canon/3_planner/plans/` |
| `polymath/vault/CLAUDE.md` | full operating guide | Replaced with pointer to `.canon` |

---

### Recovery Points

- **Remote snapshot:** `origin/develop` at `8bf5340` (pre-restructure checkpoint — "Checkpoint WIP before monorepo restructure")
- **Tag:** `pre-monorepo-restructure` pushed to remote origin
- **Local mirror clone:** taken before restructure began
- **Filesystem backups:** taken before restructure began

---

### Verification Results (2026-06-13)

| Check | Expected | Result |
|-------|----------|--------|
| `git rev-parse --show-toplevel` | `D:/VFXellence-LTD` | PASS |
| `git status --porcelain` | empty (clean tree) | PASS |
| `git check-ignore _personal archive` | both echoed | PASS |
| `git status --short \| Select-String "_personal\|/archive/"` | empty | PASS |
| `Test-Path .canon/.mission-control/client/package.json` | True | PASS |
| `Test-Path .canon/2_architect/polymath-business/ecosystems/viral` | True | PASS |
| `Test-Path .canon/.claude/skills/polymath/polymath-pitfalls/SKILL.md` | True | PASS |

All 7 checks: **PASS**

Dashboard confirmed booting on port 5174 (dev server auto-incremented from 5173 due to `.canon/.mission-control/client` path change — see DEFERRED items).

---

### Deferred Items

| Item | Notes |
|------|-------|
| **Monorepo push + remote rename** | `origin` currently points to `github.com/VFXellence-LTD/polymath-dashboard` (private). The whole monorepo would push to that repo on next push. Boss decision needed: rename existing repo (e.g. to `vfxellence`) or create a fresh repo and re-point origin. No push until Boss decides. |
| **Obsidian wikilink fix** | Vault content migrated to `.canon` contains wikilinks referencing old relative paths (e.g. `[[shared/brand-isolation/POLICY]]`). These will be broken in Obsidian until resolved. Requires a find-replace pass across migrated content or Obsidian vault root reconfiguration. |
| **Vite vault-write plugin path (Plan 1)** | `vite.config.ts` has a vault-write plugin referencing the old vault path. Needs updating to the new `.canon` path once the plugin's role in the new layout is confirmed. |
| **better-sqlite3 native build** | Dashboard depends on `better-sqlite3` which requires a native build step. Not yet verified against the new `client/` path. May need `npm rebuild` or binding regeneration. |
| **Full polymath/vault folder deletion** | `polymath/vault/` currently contains only the pointer `CLAUDE.md`. The empty directory structure (if any) can be cleaned up once Boss confirms the pointer approach is permanent. |

---

## Plan 1 Tasks 9–11 — Mission Control Server wiring, hook migration, dev boot

### Task 9 — Server index.ts, config.ts, vault route, WS stub (commit `49d5b27`)

- `config.ts`: PORT 4500, DB_PATH, CANON_PATH, VAULT_PATH, VAULT_LAUNCHES_DIR (env-overridable; defaults to `.canon/4_orchestrator/projects/polymath-business/launches/`)
- `createApp(deps)` mounts all 8 API routers + `/api/health`
- `/api/vault/launches/:filename` POST route with path-traversal guard
- WS stub on `/ws` (accept-only; full WS deferred)
- `startServer()` with file DB singleton + graceful SIGTERM/SIGINT shutdown
- 48 tests green (8 files); smoke-boot confirmed: 4500 LISTENING, `/api/transactions` 200 `[]`, `/api/tools` 200 seeded JSON

### Task 10 — Migrate 4 hooks from localStorage to fetch (commit `b87d910`)

- New `src/lib/api.ts`: typed fetch wrapper over `/api` (vite-proxied to 4500)
- `useTransactions`, `useTools`, `useSetupProgress`, `useLaunchProgress` fully rewritten to fetch on mount + optimistic mutators
- Exact hook return shapes preserved (no call-site changes needed)
- `vite.config.ts`: added `/api` proxy to 4500; rewired `vaultWritePlugin` from direct disk write to `POST /api/vault/launches/:filename`
- `tsc -b` clean (zero errors)

### Task 11 — concurrently dev script + workspace (commit `8e7cad4`, `d902b0d`)

- `.mission-control/package.json`: `pnpm dev` boots server (4500) + client (5174) via concurrently
- `pnpm-workspace.yaml`: declares `server` and `client` as workspace packages (required for correct dep hoisting)
- `pnpm.onlyBuiltDependencies: [better-sqlite3]` at workspace root (removed duplicate from server package.json)
- End-to-end boot verification PASSED: 4500 LISTENING, 5174 LISTENING, `/api/tools` 200 seeded data, client `/` 200 HTML

### Decision flagged (Task 9 note)

`config.vaultLaunchesDir` default set to `.canon/4_orchestrator/projects/polymath-business/launches/` (the resolved canon path). The old `vaultWritePlugin` wrote to `../../vault/controller/launches` relative to client — that path no longer exists post-restructure. Default updated to match actual vault layout. Boss confirmation not strictly required (env-overridable), but noted here.
